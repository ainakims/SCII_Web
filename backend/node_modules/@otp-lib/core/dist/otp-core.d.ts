/**
 * Enum HashAlgorithms
 * used to implements the hash algorithms, indexed by compact format to NIST format
 *
 * @export
 * @enum {string}
 */
declare enum HashAlgorithms {
    /**
     * The Secure Hash Algorithm 512 bits
     *
     * @type {string}
     */
    SHA512 = "SHA-512",
    /**
     * The Secure Hash Algorithm 384 bits
     *
     * @type {string}
     */
    SHA384 = "SHA-384",
    /**
     * The Secure Hash Algorithm 256 bits
     *
     * @type {string}
     */
    SHA256 = "SHA-256",
    /**
     * The Secure Hash Algorithm 160 bits
     *
     * @type {string}
     */
    SHA1 = "SHA-1"
}
/**
 * Recommended key size in bytes for each hash algorithm
 *
 * @export
 * @constant
 * @type {Readonly<Record<HashAlgorithms, number>>}
 * @see https://datatracker.ietf.org/doc/html/rfc2104#section-3
 */
declare const KeySizes: Readonly<Record<HashAlgorithms, number>>;

/**
 * Class Secret
 * used to implements the secret
 *
 * @class Secret
 */
declare class Secret {
    /**
     * The raw binary of secret
     *
     * @private
     * @readonly
     * @type {Uint8Array<ArrayBuffer>}
     * @memberof Secret
     */
    private readonly bytes;
    /**
     * Constructor of Secret class
     *
     * @private
     * @param {Uint8Array<ArrayBuffer>} bytes The raw bytes of secret
     * @memberof Secret
     */
    private constructor();
    /**
     * Create Secret from text encoder
     *
     * @private
     * @static
     * @param {string} secret The encoding text of secret
     * @param {IEncoding} encoder The encoder of secret
     * @return {Secret}
     * @memberof Secret
     */
    private static createFrom;
    /**
     * Create Secret from bytes length
     *
     * @static
     * @param {number} [length=20] The bytes length of secret
     * @return {Secret}
     * @memberof Secret
     */
    static create(length?: number): Secret;
    /**
     * Create Secret from raw binary
     *
     * @static
     * @param {Uint8Array<ArrayBuffer>} secret The raw bytes of secret
     * @return {Secret}
     * @memberof Secret
     */
    static fromBytes(secret: Uint8Array<ArrayBuffer>): Secret;
    /**
     * Create Secret from base64url text
     *
     * @static
     * @param {string} secret The base64url text of secret
     * @return {Secret}
     * @memberof Secret
     */
    static fromBase64URL(secret: string): Secret;
    /**
     * Create Secret from base64 text
     *
     * @static
     * @param {string} secret The base64 text of secret
     * @return {Secret}
     * @memberof Secret
     */
    static fromBase64(secret: string): Secret;
    /**
     * Create Secret from base32 text
     *
     * @static
     * @param {string} secret The base32 text of secret
     * @return {Secret}
     * @memberof Secret
     */
    static fromBase32(secret: string): Secret;
    /**
     * Create Secret from latin1 text
     *
     * @static
     * @param {string} secret The latin1 text of secret
     * @return {Secret}
     * @memberof Secret
     */
    static fromLatin1(secret: string): Secret;
    /**
     * Create Secret from ascii text
     *
     * @static
     * @param {string} secret The ascii text of secret
     * @return {Secret}
     * @memberof Secret
     */
    static fromAscii(secret: string): Secret;
    /**
     * Create Secret from utf8 text
     *
     * @static
     * @param {string} secret The utf8 text of secret
     * @return {Secret}
     * @memberof Secret
     */
    static fromUtf8(secret: string): Secret;
    /**
     * Create Secret from hexadecimal text
     *
     * @static
     * @param {string} secret The hexadecimal text of secret
     * @return {Secret}
     * @memberof Secret
     */
    static fromHex(secret: string): Secret;
    /**
     * Get the raw binary of secret
     *
     * @return {Uint8Array<ArrayBuffer>}
     * @memberof Secret
     */
    toBytes(): Uint8Array<ArrayBuffer>;
    /**
     * Get the base64url text of secret
     *
     * @return {string}
     * @memberof Secret
     */
    toBase64URL(): string;
    /**
     * Get the base64 text of secret
     *
     * @return {string}
     * @memberof Secret
     */
    toBase64(): string;
    /**
     * Get the base32 text of secret
     *
     * @return {string}
     * @memberof Secret
     */
    toBase32(): string;
    /**
     * Get the latin1 text of secret
     *
     * @return {string}
     * @memberof Secret
     */
    toLatin1(): string;
    /**
     * Get the ascii text of secret
     *
     * @return {string}
     * @memberof Secret
     */
    toAscii(): string;
    /**
     * Get the utf8 text of secret
     *
     * @return {string}
     * @memberof Secret
     */
    toUtf8(): string;
    /**
     * Get the hexadecimal text of secret
     *
     * @return {string}
     * @memberof Secret
     */
    toHex(): string;
}

/**
 * Interface IOTPOptions
 * used to represents the One-Time Password options
 *
 * @export
 * @interface IOTPOptions
 */
interface IOTPOptions {
    /**
     * The hash algorithm of OTP
     *
     * @type {HashAlgorithms}
     * @memberof IOTPOptions
     */
    algorithm?: HashAlgorithms;
    /**
     * The digits length of OTP
     *
     * @type {number}
     * @memberof IOTPOptions
     */
    digits?: number;
    /**
     * The secret of OTP
     *
     * @type {Secret}
     * @memberof IOTPOptions
     */
    secret?: Secret;
}

/**
 * Class OTP
 * used to implements the One-Time Password
 *
 * @class OTP
 */
declare class OTP {
    /**
     * The Crypto Subtle key of OTP
     *
     * @private
     * @type {(CryptoKey | null)}
     * @memberof OTP
     */
    private cryptoKey;
    /**
     * The hash algorithm of OTP
     *
     * @private
     * @type {HashAlgorithms}
     * @memberof OTP
     */
    private algorithm;
    /**
     * The digits length of OTP
     *
     * @private
     * @type {number}
     * @memberof OTP
     */
    private digits;
    /**
     * The secret of OTP
     *
     * @private
     * @type {Secret}
     * @memberof OTP
     */
    private secret;
    /**
     * Constructor of OTP class
     *
     * @param {IOTPOptions} [options={}] The options of OTP
     * @memberof OTP
     */
    constructor(options?: IOTPOptions);
    /**
     * Get the hash algorithm of OTP
     *
     * @return {HashAlgorithms}
     * @memberof OTP
     */
    getAlgorithm(): HashAlgorithms;
    /**
     * Get the digits length of OTP
     *
     * @return {number}
     * @memberof OTP
     */
    getDigits(): number;
    /**
     * Get the secret of OTP
     *
     * @return {Secret}
     * @memberof OTP
     */
    getSecret(): Secret;
    /**
     * Constant time comparison of two strings to prevent timing attacks
     *
     * @protected
     * @param {string} a The first string to compare
     * @param {string} b The second string to compare
     * @return {boolean}
     * @memberof OTP
     */
    protected timingSafeEqual(a: string, b: string): boolean;
    /**
     * Get the Web Crypto key of OTP
     *
     * @protected
     * @return {Promise<CryptoKey>}
     * @memberof OTP
     */
    protected getCryptoKey(): Promise<CryptoKey>;
    /**
     * Generate the HMAC digest for the counter value
     * {@link https://datatracker.ietf.org/doc/html/rfc4226#section-5.3|RFC 4226}
     *
     * @protected
     * @param {number} counter The counter value of OTP
     * @return {Promise<Uint8Array<ArrayBuffer>>}
     * @memberof OTP
     */
    protected digest(counter: number): Promise<Uint8Array<ArrayBuffer>>;
    /**
     * Create the OTP code from counter value
     * {@link https://datatracker.ietf.org/doc/html/rfc4226#section-5.4|RFC 4226}
     *
     * @protected
     * @param {number} counter The counter value of OTP
     * @return {Promise<string>}
     * @memberof OTP
     */
    protected code(counter: number): Promise<string>;
    /**
     * Verify the OTP code delta for the counter value
     *
     * @protected
     * @param {string} code The OTP code to verify
     * @param {Map<number, number>} counters The OTP counter values to verify
     * @return {Promise<number | null>}
     * @memberof OTP
     */
    protected verifyDelta(code: string, counters: Map<number, number>): Promise<number | null>;
}

/**
 * Interface IHOTPOptions
 * used to represents the HMAC-Based One-Time Password options
 *
 * @export
 * @interface IHOTPOptions
 * @extends {IOTPOptions}
 */
interface IHOTPOptions extends IOTPOptions {
    /**
     * The look-ahead window of HOTP
     *
     * @type {number}
     * @memberof IHOTPOptions
     */
    lookAhead?: number;
    /**
     * The counter value of HOTP
     *
     * @type {number}
     * @memberof IHOTPOptions
     */
    counter?: number;
}

/**
 * Class HOTP
 * used to implements the HMAC-Based One-Time Password
 *
 * @class HOTP
 * @extends {OTP}
 * @see https://datatracker.ietf.org/doc/html/rfc4226
 */
declare class HOTP extends OTP {
    /**
     * The look-ahead window of HOTP
     *
     * @private
     * @type {number}
     * @memberof HOTP
     */
    private lookAhead;
    /**
     * The counter value of HOTP
     *
     * @private
     * @type {number}
     * @memberof HOTP
     */
    private counter;
    /**
     * Constructor of HOTP class
     *
     * @param {IHOTPOptions} [options={}] The options of HOTP
     * @memberof HOTP
     */
    constructor(options?: IHOTPOptions);
    /**
     * Set the counter value of HOTP
     *
     * @param {number} counter The counter value of HOTP
     * @memberof HOTP
     */
    setCounter(counter: number): void;
    /**
     * Get the look-ahead window of HOTP
     *
     * @return {number}
     * @memberof HOTP
     */
    getLookAhead(): number;
    /**
     * Get the counter value of HOTP
     *
     * @return {number}
     * @memberof HOTP
     */
    getCounter(): number;
    /**
     * Generate the HOTP code from current counter value
     *
     * @return {Promise<string>}
     * @memberof HOTP
     */
    generate(): Promise<string>;
    /**
     * Verify the HOTP code delta for the current counter value
     *
     * @param {string} code The HOTP code to verify
     * @return {Promise<number | null>}
     * @memberof HOTP
     */
    verifyDelta(code: string): Promise<number | null>;
    /**
     * Verify if the HOTP code is valid for the current counter value
     *
     * @param {string} code The HOTP code to verify
     * @return {Promise<boolean>}
     * @memberof HOTP
     */
    verify(code: string): Promise<boolean>;
}

/**
 * Interface ITOTPWindow
 * used to represents the Time-Based One-Time Password period window
 *
 * @export
 * @interface ITOTPWindow
 */
interface ITOTPWindow {
    /**
     * The future of period window
     *
     * @type {number}
     * @memberof ITOTPWindow
     */
    future?: number;
    /**
     * The past of period window
     *
     * @type {number}
     * @memberof ITOTPWindow
     */
    past?: number;
}
/**
 * Interface ITOTPOptions
 * used to represents the Time-Based One-Time Password options
 *
 * @export
 * @interface ITOTPOptions
 * @extends {IOTPOptions}
 */
interface ITOTPOptions extends IOTPOptions {
    /**
     * The future, past period window of TOTP
     *
     * @type {(number | [number, number])}
     * @memberof TOTP
     */
    window?: number | [number, number];
    /**
     * The period in seconds of TOTP
     *
     * @type {number}
     * @memberof TOTP
     */
    period?: number;
}

/**
 * Class TOTP
 * used to implements the Time-Based One-Time Password
 *
 * @class TOTP
 * @extends {OTP}
 * @see https://datatracker.ietf.org/doc/html/rfc6238
 */
declare class TOTP extends OTP {
    /**
     * The future, past period window of TOTP
     *
     * @private
     * @type {[number, number]}
     * @memberof TOTP
     */
    private window;
    /**
     * The period in seconds of TOTP
     *
     * @private
     * @type {number}
     * @memberof TOTP
     */
    private period;
    /**
     * Constructor of TOTP class
     *
     * @param {ITOTPOptions} [options={}] The options of TOTP
     * @memberof TOTP
     */
    constructor(options?: ITOTPOptions);
    /**
     * Get the current Unix timestamp
     *
     * @private
     * @return {number}
     * @memberof TOTP
     */
    private getTimestamp;
    /**
     * Get the past, future period window of TOTP
     *
     * @return {number}
     * @memberof TOTP
     */
    getWindow(): [number, number];
    /**
     * Get the period in seconds of TOTP
     *
     * @return {number}
     * @memberof TOTP
     */
    getPeriod(): number;
    /**
     * Get the counter of TOTP
     *
     * @return {number}
     * @memberof TOTP
     */
    getCounter(): number;
    /**
     * Get the number of seconds used in the current time step
     *
     * @return {number}
     * @memberof TOTP
     */
    getTimeUsed(): number;
    /**
     * Get the remaining time in seconds until the next time step
     *
     * @return {number}
     * @memberof TOTP
     */
    getTimeRemaining(): number;
    /**
     * Generate the TOTP code from current counter time step
     *
     * @return {Promise<string>}
     * @memberof TOTP
     */
    generate(): Promise<string>;
    /**
     * Verify the TOTP code delta for the current counter value
     *
     * @param {string} code The TOTP code to verify
     * @return {Promise<number | null>}
     * @memberof TOTP
     */
    verifyDelta(code: string): Promise<number | null>;
    /**
     * Verify if the TOTP code is valid for the current counter value
     *
     * @param {string} code The TOTP code to verify
     * @return {Promise<boolean>}
     * @memberof TOTP
     */
    verify(code: string): Promise<boolean>;
}

export { HOTP, HashAlgorithms, type IHOTPOptions, type IOTPOptions, type ITOTPOptions, type ITOTPWindow, KeySizes, OTP, Secret, TOTP };

/**
 * @otp-lib/authenticator v0.2.1
 * Copyright (c) 2025 Unknown-Robot
 * https://github.com/Unknown-Robot/otp-lib/tree/main/packages/authenticator#readme
 * MIT License
 */
var OTPAuth = function(exports) {
    "use strict";
    var HashAlgorithms = (HashAlgorithms2 => {
        HashAlgorithms2["SHA512"] = "SHA-512";
        HashAlgorithms2["SHA384"] = "SHA-384";
        HashAlgorithms2["SHA256"] = "SHA-256";
        HashAlgorithms2["SHA1"] = "SHA-1";
        return HashAlgorithms2;
    })(HashAlgorithms || {});
    var KeySizes = {
        ["SHA-512"]: 64,
        ["SHA-384"]: 48,
        ["SHA-256"]: 32,
        ["SHA-1"]: 20
    };
    var TypeGuard = class _TypeGuard {
        static isEnum(value, enumeration) {
            return Object.values(enumeration).includes(value);
        }
        static isKeyOf(value, reference) {
            return reference.hasOwnProperty(value);
        }
        static isArrayOf(value, predicate) {
            return _TypeGuard.isArray(value) && value.every(predicate);
        }
        static isPlainObject(value) {
            return typeof value === "object" && value !== null && Object.getPrototypeOf(value) === Object.prototype;
        }
        static isArray(value) {
            return Array.isArray(value);
        }
        static isNonNegativeInteger(value) {
            return _TypeGuard.isInteger(value) && value >= 0;
        }
        static isPositiveInteger(value) {
            return _TypeGuard.isInteger(value) && value > 0;
        }
        static isInteger(value) {
            return Number.isInteger(value);
        }
        static isString(value) {
            return typeof value === "string";
        }
    };
    var typeguard_default = TypeGuard;
    var Base64 = class {
        static encode(bytes) {
            const value = new Array(bytes.length);
            for (let i = 0; i < bytes.length; i++) {
                value[i] = String.fromCharCode(bytes[i]);
            }
            return btoa(value.join(""));
        }
        static decode(text) {
            const binary = atob(text);
            const bytes = new Uint8Array(binary.length);
            for (let i = 0; i < binary.length; i++) {
                bytes[i] = binary.charCodeAt(i);
            }
            return bytes;
        }
    };
    var base64_default = Base64;
    var Base64URL = class extends base64_default {
        static encode(bytes) {
            const base64 = super.encode(bytes);
            return base64.replaceAll("+", "-").replaceAll("/", "_").replaceAll("=", "");
        }
        static decode(text) {
            const base64 = text.replaceAll("-", "+").replaceAll("_", "/");
            return super.decode(base64);
        }
    };
    var base64url_default = Base64URL;
    var Base32 = class {
        static alphabet="ABCDEFGHIJKLMNOPQRSTUVWXYZ234567";
        static bitsPerGroup=5;
        static bitsPerByte=8;
        static padding="=";
        static encode(bytes) {
            let bits = 0, buffer = 0, result = "";
            for (const byte of bytes) {
                buffer = buffer << this.bitsPerByte | byte;
                bits += this.bitsPerByte;
                while (bits >= this.bitsPerGroup) {
                    result += this.alphabet[buffer >> bits - this.bitsPerGroup & 31];
                    bits -= this.bitsPerGroup;
                }
            }
            if (bits > 0) {
                result += this.alphabet[buffer << this.bitsPerGroup - bits & 31];
            }
            while (result.length % this.bitsPerByte !== 0) {
                result += this.padding;
            }
            return result;
        }
        static decode(text) {
            const value = text.replaceAll(this.padding, "").toUpperCase();
            const length = value.length * this.bitsPerGroup / this.bitsPerByte;
            const bytes = new Uint8Array(length);
            let bits = 0, buffer = 0, index = 0;
            for (let i = 0; i < value.length; i++) {
                const character = this.alphabet.indexOf(value[i]);
                if (character === -1) {
                    throw new Error(`The base32 character "${value[i]}" at position ${i + 1} is invalid`);
                }
                buffer = buffer << this.bitsPerGroup | character;
                bits += this.bitsPerGroup;
                if (bits >= this.bitsPerByte) {
                    bits -= this.bitsPerByte;
                    bytes[index++] = buffer >> bits & 255;
                }
            }
            return bytes;
        }
    };
    var base32_default = Base32;
    var Latin1 = class {
        static encode(bytes) {
            const value = new Array(bytes.length);
            for (let i = 0; i < bytes.length; i++) {
                value[i] = String.fromCharCode(bytes[i]);
            }
            return value.join("");
        }
        static decode(text) {
            const bytes = new Uint8Array(text.length);
            for (let i = 0; i < text.length; i++) {
                const code = text.charCodeAt(i);
                if (code > 255) {
                    throw new Error(`The latin1 character "${text[i]}" at position ${i + 1} is invalid`);
                }
                bytes[i] = code;
            }
            return bytes;
        }
    };
    var latin1_default = Latin1;
    var Ascii = class {
        static encode(bytes) {
            const value = new Array(bytes.length);
            for (let i = 0; i < bytes.length; i++) {
                value[i] = String.fromCharCode(bytes[i] & 127);
            }
            return value.join("");
        }
        static decode(text) {
            const bytes = new Uint8Array(text.length);
            for (let i = 0; i < text.length; i++) {
                const code = text.charCodeAt(i);
                if (code > 127) {
                    throw new Error(`The ascii character "${text[i]}" at position ${i + 1} is invalid`);
                }
                bytes[i] = code;
            }
            return bytes;
        }
    };
    var ascii_default = Ascii;
    var Utf8 = class {
        static decoder=new TextDecoder("utf-8");
        static encoder=new TextEncoder;
        static encode(bytes) {
            return this.decoder.decode(bytes);
        }
        static decode(text) {
            return this.encoder.encode(text);
        }
    };
    var utf8_default = Utf8;
    var Hex = class {
        static alphabet=Array.from({
            length: 256
        }, (_, i) => i.toString(16).padStart(2, "0"));
        static encode(bytes) {
            const value = new Array(bytes.length);
            for (let i = 0; i < bytes.length; i++) {
                value[i] = this.alphabet[bytes[i]];
            }
            return value.join("");
        }
        static decode(text) {
            if (text.length % 2 !== 0) {
                throw new Error("The hexadecimal text length must be even");
            }
            const bytes = new Uint8Array(text.length / 2);
            for (let i = 0; i < text.length; i += 2) {
                const chunk = text.substring(i, i + 2).toLowerCase();
                if (!this.alphabet.includes(chunk)) {
                    throw new Error(`The hexadecimal character "${chunk}" at position ${i + 1} is invalid`);
                }
                bytes[i / 2] = parseInt(chunk, 16);
            }
            return bytes;
        }
    };
    var hex_default = Hex;
    var Secret = class {
        bytes;
        constructor(bytes) {
            if (!(bytes instanceof Uint8Array)) {
                throw new Error("The bytes must be a instance of Uint8Array class");
            } else if (!bytes.byteLength) {
                throw new Error("The bytes must not be empty");
            }
            this.bytes = new Uint8Array(bytes);
        }
        static createFrom(secret, encoder) {
            if (!typeguard_default.isString(secret)) {
                throw new Error("The secret must be a string");
            }
            return new this(encoder.decode(secret));
        }
        static create(length = 20) {
            const bytes = new Uint8Array(length);
            return new this(globalThis.crypto.getRandomValues(bytes));
        }
        static fromBytes(secret) {
            return new this(secret);
        }
        static fromBase64URL(secret) {
            return this.createFrom(secret, base64url_default);
        }
        static fromBase64(secret) {
            return this.createFrom(secret, base64_default);
        }
        static fromBase32(secret) {
            return this.createFrom(secret, base32_default);
        }
        static fromLatin1(secret) {
            return this.createFrom(secret, latin1_default);
        }
        static fromAscii(secret) {
            return this.createFrom(secret, ascii_default);
        }
        static fromUtf8(secret) {
            return this.createFrom(secret, utf8_default);
        }
        static fromHex(secret) {
            return this.createFrom(secret, hex_default);
        }
        toBytes() {
            return new Uint8Array(this.bytes);
        }
        toBase64URL() {
            return base64url_default.encode(this.bytes);
        }
        toBase64() {
            return base64_default.encode(this.bytes);
        }
        toBase32() {
            return base32_default.encode(this.bytes);
        }
        toLatin1() {
            return latin1_default.encode(this.bytes);
        }
        toAscii() {
            return ascii_default.encode(this.bytes);
        }
        toUtf8() {
            return utf8_default.encode(this.bytes);
        }
        toHex() {
            return hex_default.encode(this.bytes);
        }
    };
    var secret_default = Secret;
    var OTP = class {
        cryptoKey=null;
        algorithm;
        digits;
        secret;
        constructor(options = {}) {
            if (!typeguard_default.isPlainObject(options)) {
                throw new Error("The options must be a plain object");
            }
            const algorithm = options.algorithm ?? "SHA-1";
            if (!typeguard_default.isEnum(algorithm, HashAlgorithms)) {
                throw new Error(`The options.algorithm "${algorithm}" is not supported`);
            }
            const secret = options.secret ?? secret_default.create(KeySizes[algorithm]);
            if (!(secret instanceof secret_default)) {
                throw new Error("The options.secret must be a instance of Secret class");
            }
            const digits = options.digits ?? 6;
            if (!typeguard_default.isPositiveInteger(digits)) {
                throw new Error("The options.digits must be a positive integer");
            }
            this.algorithm = algorithm;
            this.secret = secret;
            this.digits = digits;
        }
        getAlgorithm() {
            return this.algorithm;
        }
        getDigits() {
            return this.digits;
        }
        getSecret() {
            return this.secret;
        }
        timingSafeEqual(a, b) {
            if (a.length !== b.length) {
                return false;
            }
            let diff = 0;
            for (let i = 0; i < a.length; i++) {
                diff |= a.charCodeAt(i) ^ b.charCodeAt(i);
            }
            return diff === 0;
        }
        async getCryptoKey() {
            if (this.cryptoKey === null) {
                this.cryptoKey = await globalThis.crypto.subtle.importKey("raw", this.secret.toBytes(), {
                    name: "HMAC",
                    hash: this.algorithm
                }, false, [ "sign" ]);
            }
            return this.cryptoKey;
        }
        async digest(counter) {
            const buffer = new ArrayBuffer(8);
            const view = new DataView(buffer);
            view.setBigUint64(0, BigInt(counter), false);
            const digest = await globalThis.crypto.subtle.sign({
                name: "HMAC",
                hash: this.algorithm
            }, await this.getCryptoKey(), buffer);
            return new Uint8Array(digest);
        }
        async code(counter) {
            const digest = await this.digest(counter);
            const offset = digest[digest.byteLength - 1] & 15;
            const code = ((digest[offset] & 127) << 24 | (digest[offset + 1] & 255) << 16 | (digest[offset + 2] & 255) << 8 | digest[offset + 3] & 255) % 10 ** this.digits;
            return code.toString().padStart(this.digits, "0");
        }
        async verifyDelta(code, counters) {
            if (code.length === this.digits) {
                for (const [delta, counter] of counters) {
                    const value = await this.code(counter);
                    if (this.timingSafeEqual(value, code)) {
                        return delta;
                    }
                }
            }
            return null;
        }
    };
    var otp_default = OTP;
    var HOTP = class extends otp_default {
        lookAhead;
        counter;
        constructor(options = {}) {
            super(options);
            const lookAhead = options.lookAhead ?? 0;
            if (!typeguard_default.isNonNegativeInteger(lookAhead)) {
                throw new Error("The options.lookAhead must be non-negative integer");
            }
            const counter = options.counter ?? 0;
            if (!typeguard_default.isNonNegativeInteger(counter)) {
                throw new Error("The options.counter must be non-negative integer");
            }
            this.lookAhead = lookAhead;
            this.counter = counter;
        }
        setCounter(counter) {
            this.counter = Math.abs(counter);
        }
        getLookAhead() {
            return this.lookAhead;
        }
        getCounter() {
            return this.counter;
        }
        generate() {
            return super.code(this.counter);
        }
        verifyDelta(code) {
            const counters = new Map;
            for (let i = 0; i <= this.lookAhead; i++) {
                counters.set(i, this.counter + i);
            }
            return super.verifyDelta(code, counters);
        }
        async verify(code) {
            return await this.verifyDelta(code) !== null;
        }
    };
    var hotp_default = HOTP;
    var TOTP = class extends otp_default {
        window;
        period;
        constructor(options = {}) {
            super(options);
            const window = options.window ?? [ 0, 0 ];
            if ((!typeguard_default.isArrayOf(window, typeguard_default.isNonNegativeInteger) || window.length !== 2) && !typeguard_default.isNonNegativeInteger(window)) {
                throw new Error("The options.window must be a non-negative integer or an array of two non-negative integers");
            }
            const period = options.period ?? 30;
            if (!typeguard_default.isPositiveInteger(period)) {
                throw new Error("The options.period must be a positive integer");
            }
            this.window = !typeguard_default.isArray(window) ? [ window, window ] : window;
            this.period = period;
        }
        getTimestamp() {
            return Math.floor(Date.now() / 1e3);
        }
        getWindow() {
            return this.window;
        }
        getPeriod() {
            return this.period;
        }
        getCounter() {
            return Math.floor(this.getTimestamp() / this.period);
        }
        getTimeUsed() {
            return this.getTimestamp() % this.period;
        }
        getTimeRemaining() {
            return this.period - this.getTimeUsed();
        }
        async generate() {
            return super.code(this.getCounter());
        }
        verifyDelta(code) {
            const [past, future] = this.getWindow();
            const counter = this.getCounter();
            const counters = new Map([ [ 0, counter ] ]);
            for (let i = 1; i <= Math.max(past, future); i++) {
                if (i <= past) {
                    counters.set(-i, counter - i);
                }
                if (i <= future) {
                    counters.set(i, counter + i);
                }
            }
            return super.verifyDelta(code, counters);
        }
        async verify(code) {
            return await this.verifyDelta(code) !== null;
        }
    };
    var totp_default = TOTP;
    var TypeGuard2 = class _TypeGuard2 {
        static isEnum(value, enumeration) {
            return Object.values(enumeration).includes(value);
        }
        static isKeyOf(value, reference) {
            return reference.hasOwnProperty(value);
        }
        static isArrayOf(value, predicate) {
            return _TypeGuard2.isArray(value) && value.every(predicate);
        }
        static isPlainObject(value) {
            return typeof value === "object" && value !== null && Object.getPrototypeOf(value) === Object.prototype;
        }
        static isArray(value) {
            return Array.isArray(value);
        }
        static isNonNegativeInteger(value) {
            return _TypeGuard2.isInteger(value) && value >= 0;
        }
        static isPositiveInteger(value) {
            return _TypeGuard2.isInteger(value) && value > 0;
        }
        static isInteger(value) {
            return Number.isInteger(value);
        }
        static isString(value) {
            return typeof value === "string";
        }
    };
    var typeguard_default2 = TypeGuard2;
    var AuthenticatorType = (AuthenticatorType2 => {
        AuthenticatorType2["HOTP"] = "hotp";
        AuthenticatorType2["TOTP"] = "totp";
        return AuthenticatorType2;
    })(AuthenticatorType || {});
    var Authenticator = class {
        account;
        issuer;
        otp;
        constructor(otp, options) {
            if (!typeguard_default2.isPlainObject(options)) {
                throw new Error("The options must be a plain object");
            }
            const account = options.account;
            if (!typeguard_default2.isString(account)) {
                throw new Error("The options.account must be a string");
            }
            const issuer = options.issuer ?? null;
            if (issuer !== null && !typeguard_default2.isString(issuer)) {
                throw new Error("The options.issuer must be a string or null");
            }
            this.account = account;
            this.issuer = issuer;
            this.otp = otp;
        }
        getAlgorithm() {
            return this.otp.getAlgorithm();
        }
        getDigits() {
            return this.otp.getDigits();
        }
        getSecret() {
            return this.otp.getSecret();
        }
        getAccount() {
            return this.account;
        }
        getIssuer() {
            return this.issuer;
        }
        static fromURL(url) {
            if (!url.protocol.startsWith("otpauth")) {
                throw new Error("The authenticator URI protocol is invalid");
            } else if (!typeguard_default2.isEnum(url.host, AuthenticatorType)) {
                throw new Error("The authenticator URI type is invalid");
            }
            const label = decodeURIComponent(url.pathname.substring(1));
            if (!label.length) {
                throw new Error("The authenticator URI label is invalid");
            }
            const options = {
                account: label
            };
            const separator = label.indexOf(":");
            if (separator !== -1) {
                options.account = label.substring(separator + 1);
            }
            const issuer = url.searchParams.get("issuer");
            if (issuer !== null) {
                options.issuer = issuer;
            } else if (separator > 0) {
                options.issuer = label.substring(0, separator);
            }
            const secret = url.searchParams.get("secret");
            if (!secret || !secret.length) {
                throw new Error("The authenticator URI secret is required");
            }
            options.secret = secret_default.fromBase32(secret);
            const algorithm = url.searchParams.get("algorithm");
            if (algorithm !== null) {
                const value = algorithm.toUpperCase();
                if (!typeguard_default2.isKeyOf(value, HashAlgorithms)) {
                    throw new Error("The authenticator URI algorithm is not supported");
                }
                options.algorithm = HashAlgorithms[value];
            }
            const digits = url.searchParams.get("digits");
            if (digits !== null) {
                const value = parseInt(digits, 10);
                if (!typeguard_default2.isPositiveInteger(value)) {
                    throw new Error("The authenticator URI digits is invalid");
                }
                options.digits = value;
            }
            return options;
        }
        toURL() {
            const url = new URL("otpauth://");
            if (this.issuer) {
                url.pathname = [ this.issuer, this.account ].map(encodeURIComponent).join(":");
                url.searchParams.set("issuer", this.issuer);
            } else {
                url.pathname = encodeURIComponent(this.account);
            }
            const algorithm = this.getAlgorithm();
            const secret = this.getSecret();
            const digits = this.getDigits();
            url.searchParams.set("algorithm", algorithm.replace("-", ""));
            url.searchParams.set("secret", secret.toBase32());
            url.searchParams.set("digits", digits.toString());
            return url;
        }
    };
    var authenticator_default = Authenticator;
    var HOTPAuthenticator = class extends authenticator_default {
        constructor(options) {
            super(new hotp_default(options), options);
        }
        setCounter(counter) {
            return this.otp.setCounter(counter);
        }
        getLookAhead() {
            return this.otp.getLookAhead();
        }
        getCounter() {
            return this.otp.getCounter();
        }
        generate() {
            return this.otp.generate();
        }
        verifyDelta(code) {
            return this.otp.verifyDelta(code);
        }
        async verify(code) {
            return this.otp.verify(code);
        }
        static fromURI(uri) {
            const url = new URL(uri);
            if (url.host !== "hotp") {
                throw new Error("The authenticator URI type is invalid");
            }
            const options = super.fromURL(url);
            const counter = url.searchParams.get("counter");
            if (counter !== null) {
                const value = parseInt(counter, 10);
                if (!typeguard_default2.isPositiveInteger(value)) {
                    throw new Error("The authenticator URI counter is invalid");
                }
                options.counter = value;
            } else {
                throw new Error("The authenticator URI counter is required");
            }
            return new this(options);
        }
        toURI() {
            const url = super.toURL();
            url.host = "hotp";
            const counter = this.getCounter();
            url.searchParams.set("counter", counter.toString());
            return url.toString();
        }
    };
    var hotp_default2 = HOTPAuthenticator;
    var TOTPAuthenticator = class extends authenticator_default {
        constructor(options) {
            super(new totp_default(options), options);
        }
        getWindow() {
            return this.otp.getWindow();
        }
        getPeriod() {
            return this.otp.getPeriod();
        }
        getCounter() {
            return this.otp.getCounter();
        }
        getTimeUsed() {
            return this.otp.getTimeUsed();
        }
        getTimeRemaining() {
            return this.otp.getTimeRemaining();
        }
        generate() {
            return this.otp.generate();
        }
        verifyDelta(code) {
            return this.otp.verifyDelta(code);
        }
        async verify(code) {
            return this.otp.verify(code);
        }
        static fromURI(uri) {
            const url = new URL(uri);
            if (url.host !== "totp") {
                throw new Error("The authenticator URI type is invalid");
            }
            const options = super.fromURL(url);
            const period = url.searchParams.get("period");
            if (period !== null) {
                const value = parseInt(period, 10);
                if (!typeguard_default2.isPositiveInteger(value)) {
                    throw new Error("The authenticator URI period is invalid");
                }
                options.period = value;
            }
            return new this(options);
        }
        toURI() {
            const url = super.toURL();
            url.host = "totp";
            const period = this.getPeriod();
            url.searchParams.set("period", period.toString());
            return url.toString();
        }
    };
    var totp_default2 = TOTPAuthenticator;
    exports.Authenticator = authenticator_default;
    exports.AuthenticatorType = AuthenticatorType;
    exports.HOTP = hotp_default2;
    exports.HashAlgorithms = HashAlgorithms;
    exports.Secret = secret_default;
    exports.TOTP = totp_default2;
    return exports;
}({});//# sourceMappingURL=otp-auth.iife.js.map